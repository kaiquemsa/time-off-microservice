# Project Time Off

This repository contains the frontend and backend for the Time Off project.

The main entry point is `time-off-service`. It includes everything required to run, configure, and understand the core application behavior.

The `time-off-frontend` folder contains the UI layer that integrates with the service.
